# Python-generated files
__pycache__/
*.py[oc]
*.pyc
*.pyo
*.pyd
build/
dist/
wheels/
*.egg-info
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/

# Jupyter lab
*.ipynb
.ipynb_checkpoints/

# notebooks/
notebooks/**/.ipynb_checkpoints/
notebooks/**/*.ipynb

# data types
*.csv
*.parquet

.env

# Modelo Random Forest entrenado localmente (binario regenerable, ver
# ml/train_random_forest.py). Las métricas sí se versionan
# (ml/metrics_random_forest.json) como documentación del resultado.
ml/model_random_forest.pkl

# Modelo XGBoost entrenado localmente (binario regenerable, ver
# ml/train_xgboost.py). Las métricas sí se versionan
# (ml/metrics_xgboost.json) como documentación del resultado.
ml/model_xgboost.pkl

# Modelo Regresión Logística entrenado localmente (binario regenerable, ver
# ml/train_logistic_regression.py). Las métricas sí se versionan
# (ml/metrics_logistic_regression.json) como documentación del resultado.
ml/model_logistic_regression.pkl

# Modelo Gradient Boosting entrenado localmente (binario regenerable, ver
# ml/train_gradient_boosting.py). Las métricas sí se versionan
# (ml/metrics_gradient_boosting.json) como documentación del resultado.
ml/model_gradient_boosting.pkl

# Resultados de cada modelo
ml/*.json